var should = require('chai').should;
var processAndTest = require('../src/process-and-test.js');

describe(`ensure it's looping through syncronous processes correctly`, function() {

	before('init a new process, add some processes to it', function() {
		var sampleProcess = new processAndTest('this is a string data im sending');

	})

});

describe(`ensure it's looping through asyncronous processes correctly`, function() {

	before('init a new process, add some processes to it', function() {
		var sampleProcess = new processAndTest('this is a string data im sending');

	})

});

describe(`ensure it's looping through both sync and async processes correctly`, function() {

	before('init a new process, add some processes to it', function() {
		var sampleProcess = new processAndTest('this is a string data im sending');

	})

});